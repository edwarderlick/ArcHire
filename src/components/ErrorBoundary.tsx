// Placeholder: ErrorBoundary requires @types/react for class component support.
// The blank-screen crash is prevented by the wagmi config fallback in src/config/wagmi.ts.
// Install @types/react to enable a proper ErrorBoundary if needed.
